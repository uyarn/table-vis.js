'use strict';

import drawPoint from './drawPoint';


const point = function(ori,data,index,direct,title,titleIdx){

    drawPoint(ori,data,index,direct,title,titleIdx);
}
/*!
 *  exports the point chart module.
 */
export default point;
