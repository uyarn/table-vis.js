'use strict';

import drawWordCloud from './drawWordCloud';
// insert css style to head

const wordCloud = (data,index,title,titleIdx) =>{

    drawWordCloud(data,index,title,titleIdx);
}
/*!
 *  exports the bar chart module.
 */
export default wordCloud;
